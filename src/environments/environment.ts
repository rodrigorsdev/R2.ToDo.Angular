// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyCD02EdId0bq63yqjkeSWJKKFlZLvdTTCI",
    authDomain: "r2todo-13624.firebaseapp.com",
    databaseURL: "https://r2todo-13624.firebaseio.com",
    projectId: "r2todo-13624",
    storageBucket: "r2todo-13624.appspot.com",
    messagingSenderId: "546230642483",
    appId: "1:546230642483:web:311e28cf0b04f52ce3c046"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
